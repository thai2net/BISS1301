#include <stdio.h>	// โปรแกรมที่ 6.1 แสดงการใช้งานโครงสร้างคำสั่งทางเลือก if() แบบไม่ใช้บล็อก
main() {	
         int x = 5, y = 10;	
         if(x < y)	// เมื่อ x น้อยกว่า y
                  printf("%d is a smaller than %d", x, y);	
}