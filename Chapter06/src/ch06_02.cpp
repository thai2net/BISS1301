#include <stdio.h>	// โปรแกรมที่ 6.2 แสดงการใช้งานโครงสร้างคำสั่งทางเลือก if() แบบใช้บล็อก
main() {	
         int x = 5, y = 10;	
         if(y > x) {	// เมื่อ y มากกว่า x
                 printf("%d", y);	
                 printf(" is a greater than ");	
                 printf("%d" x);	
        }	
}	