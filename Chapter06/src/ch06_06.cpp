#include <stdio.h> // โปรแกรมที่ 6.6 แสดงการใช้งานโครงสร้างคำสั่งทางเลือก switch()..case
main() {
      char grade = 'A';
      switch(grade) {
         case 'A': printf("Grade %c" is very good, grade); break;
         case 'B': printf("Grade %c" is very good, grade); break;
         case 'C': printf("Grade %c" is very good, grade); break;
         case 'D': printf("Grade %c" is very good, grade); break;
         case 'F': printf("Grade %c" is very good, grade); break;
         default: printf("%c" is not Grade, grade);
      }
}