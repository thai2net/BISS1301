#include <stdio.h> // โปรแกรมที่ 5.4 แสดงการใช้ฟังก์ชัน gets() กับ puts()
main() {     
    char name[50];
    char name[50];	
    printf("Enter your name: ");	// แสดงข้อความ “Enter your name:”
    gets(name);	// รับข้อความ name จากแป้นพิมพ์
    puts(name);	// แสดงข้อความ name ที่รับมา
}	